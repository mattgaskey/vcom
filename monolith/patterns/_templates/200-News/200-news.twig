{% extends 'base.twig' %}

{% block main_content %}
    {% include '@' ~ page_header|default('hero-page-header') with { head: "Exciting news story headline", slab_variant : header_slab|default('slab--newcity-white') } %}
    {% include '@breadcrumb-slab' %}
    {% include '@content-meta-slab' with { slab_variant : slab_variant|default('slab--newcity-white') } %}
    {% include '@sidebar-editorial' with { slab_variant : slab_variant|default('slab--newcity-white') }  %} 
{% endblock %}

